[project]
name = "bemfa-mcp"
version = "1.0.0"
description = "巴法云MQTT智能灯光控制MCP服务"
authors = [
    {name = "Your Name", email = "your.email@example.com"},
]
dependencies = [
    "paho-mqtt>=1.6.0",
]
requires-python = ">=3.8"

[project.scripts]
bemfa-mcp = "bemfa_mcp:main"
